{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://owlcoda.dev/schemas/runkit/core-artifact-v1.json",
  "title": "OwlCoda RunKit Core Artifact V1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "core", "producer", "payload", "extensions"],
  "properties": {
    "schemaVersion": { "const": "RunKitCoreArtifactV1" },
    "core": { "$ref": "engine-pin.schema.json" },
    "producer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["adapterKind", "adapterVersion"],
      "properties": {
        "adapterKind": { "type": "string", "minLength": 1 },
        "adapterVersion": { "type": "string", "minLength": 1 }
      }
    },
    "payload": { "type": "object" },
    "extensions": { "type": "object", "propertyNames": { "pattern": "^[^.]+\\..+" } }
  }
}
