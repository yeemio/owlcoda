示例代码

下面是 0.13.95 的 KNOWN_FENCE_LANGS 入口。

─── ts ─────────────────────────────────
  const KNOWN_FENCE_LANGS = new Set([
    'ts', 'tsx', 'js', 'py', 'bash',
  ])
────────────────────────────────────────

代码块结束。
