// Minimal fullscreen.ts — ink/ only uses isMouseClicksDisabled and isFullscreenEnvEnabled
export function isFullscreenEnvEnabled(): boolean {
  return true
}

export function isMouseClicksDisabled(): boolean {
  return false
}
